import styled from "styled-components";
import colors from "../../../utils/colors";

const IconLeasons = {
    Container: styled.div`
        border-radius: 8px;
        width: 48px;
        height: 48px;
        display: flex;
        justify-content: center;
        align-items: center;
        align-content: center;
        background-color: ${colors.basic}
    `
}

export default IconLeasons;